# GoogleAPI French Audio Generator

This small tool automates generating french audio translations for large word lists. It's a BYOC (Bring Your Own Client) style of tool that expects a simplified client interface, so you can just wrap your favorite (or self-made) API client and pass it into the generator function.
