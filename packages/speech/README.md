# LangDeck Speech Generator

This small tool automates generating audio for large word/phrase lists. It's a BYOC (Bring Your Own Client) style of
tool that expects a simplified client interface, so you can just wrap your favorite (or self-made) API client and pass
it into the generator function.
